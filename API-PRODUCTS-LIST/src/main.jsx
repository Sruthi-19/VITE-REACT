import React from 'react'
import ReactDOM from 'react-dom/client'
import MainDisplay from './OVERALL_PRODUCTS'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MainDisplay />
  </React.StrictMode>,
)
